// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init
// Prisma schema for buyer lead intake app with two models:
// 1. buyers - id, fullName, email, phone, city, propertyType, bhk, purpose, budgetMin, budgetMax, timeline, source, status, notes, tags, ownerId, updatedAt
// 2. buyer_history - id, buyerId, changedBy, changedAt, diff (JSON)


generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql" // or "sqlite"
  url      = env("DATABASE_URL")
}

model Buyer {
  id            String        @id @default(uuid())
  fullName      String        @db.VarChar(80)
  email         String?       @unique
  phone         String        @db.VarChar(15)
  city          City
  propertyType  PropertyType
  bhk           BHK?
  purpose       Purpose
  budgetMin     Int?
  budgetMax     Int?
  timeline      Timeline
  source        Source
  status        Status      @default(New)
  notes         String?     @db.Text
  tags          String[]
  ownerId       String
  updatedAt     DateTime    @updatedAt
  histories     BuyerHistory[]
  
  @@index([ownerId])
}

model BuyerHistory {
  id         String    @id @default(uuid())
  buyerId    String
  changedBy  String
  changedAt  DateTime  @default(now())
  diff       Json
  buyer      Buyer     @relation(fields: [buyerId], references: [id], onDelete: Cascade)

  @@index([buyerId])
}

enum City {
  Chandigarh
  Mohali
  Zirakpur
  Panchkula
  Other
}

enum PropertyType {
  Apartment
  Villa
  Plot
  Office
  Retail
}

enum BHK {
 ONE
  TWO
  THREE
  FOUR
  STUDIO
}

enum Purpose {
  Buy
  Rent
}

enum Timeline {
  ZERO_TO_THREE_M
  THREE_TO_SIX_M
  MORE_THAN_SIX_M
  EXPLORING
}

enum Source {
  Website
  Referral
  Walkin
  Call
  Other
}

enum Status {
  New
  Qualified
  Contacted
  Visited
  Negotiation
  Converted
  Dropped
}
